[project]
name = "sophnet-pptx"
version = "0.1.0"
description = "Skill-local runtime dependencies for sophnet-pptx"
requires-python = ">=3.10"
dependencies = [
  "defusedxml>=0.7.1",
  "lxml>=5.2.0",
  "markitdown[pptx]>=0.0.1a4",
  "pillow>=10.0.0",
]

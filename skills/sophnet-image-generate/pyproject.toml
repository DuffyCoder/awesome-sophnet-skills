[project]
name = "sophnet-image-generate"
version = "1.0.0"
description = "SophNet text-to-image generation with task polling"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.28.0",
    "sophnet-tools>=0.0.1",
]

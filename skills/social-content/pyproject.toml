[project]
name = "social-content"
version = "2.0.0"
description = "Social media content creation for Chinese platforms with RAG and image generation"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.28.0",
    "sophnet-tools>=0.0.1",
]
